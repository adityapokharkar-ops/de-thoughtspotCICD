name: Deploy ThoughtSpot TMLs

on:
  workflow_dispatch:
    inputs:
      deploy_qa:
        description: "Deploy to QA org"
        required: false
        type: boolean
      deploy_perf:
        description: "Deploy to PERF org"
        required: false
        type: boolean
      deploy_migt:
        description: "Deploy to MIGT org"
        required: false
        type: boolean

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: test

      - name: Deploy to QA
        if: ${{ inputs.deploy_qa }}
        run: |
          echo "Deploying ThoughtSpot TMLs to QA"
          # curl / ts-cli / python command goes here

      - name: Deploy to PERF
        if: ${{ inputs.deploy_perf }}
        run: |
          echo "Deploying ThoughtSpot TMLs to PERF"
          # curl / ts-cli / python command goes here

      - name: Deploy to MIGT
        if: ${{ inputs.deploy_migt }}
        run: |
          echo "Deploying ThoughtSpot TMLs to MIGT"
          # curl / ts-cli / python command goes here
